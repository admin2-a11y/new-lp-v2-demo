# CODEX_TASKS — 改善タスク backlog（デザイン / 情報配置 / スマホ最適化）

上から順に、**1タスクずつ**実施 → 確認（320/375/390/414px＋PC）→ コミット。
各タスクの「▶ 指示（コピペ用）」を Codex にそのまま渡してよい。
前提ルールは `AGENTS.md`（特に「壊してはいけないもの」）を必ず守ること。

**推奨着手順（2026-07-07更新・リデザイン実施が決定）**:
T0 → T1 → T2（土台の掃除）→ **R0〜R11（案A リデザイントラック、本命）** → T14 → 残タスク。
※ T3・T5・T8・T9・T10・T13 は Rタスクに統合済み（各Rタスクの【関連】参照）。T4・T6・T7・T11・T12 は R2/R3 実施時に実質カバーされるが、単独で先行実施しても矛盾しない。

---

## 現状の把握（監査結果・2026-07-07時点）
- モバイルファースト設計（主ブレークポイント `max-width:768px`）。375px幅で**横スクロール崩れは無し**（土台は良好）。
- 一方で改善余地:
  - **viewportメタがページ間で不統一**（`user-scalable=no` / `maximum-scale=1.0` が混在＝ズーム禁止でアクセシビリティ的に不可）。
  - **固定px幅が約256箇所** → 320px級の狭い端末や将来の改修で崩れやすい。
  - 比較表・情報量が多く、モバイルでの**情報の優先順位/余白**に伸びしろ。

---

## T0. 壊れコードのクイック修正（最優先・低リスク）
**目的**: 明確に壊れている記述を先に直し、以降の改修の土台をきれいにする。見た目の回帰リスクほぼ無し。
▶ 指示（コピペ用）:
> 以下の壊れコードを修正して。**アンケートのid/name/構造は触らない**こと。
> 1. 全7ファイル（index.html / result.php / beginner.php / beginner_result.php / redirect.php / operationinfo.php）のヘッダースクロールJS内 `rgba(252,128,3,,0.8)` と `rgba(252,128,3,,1)` のカンマ二重を修正（`rgba(252,128,3,0.8)` / `rgba(252,128,3,1)`）。
> 2. index.html と result.php のモーダルスライダーJS内 `rgba(241,228,215,, 0.19)` も同様に修正。
> 3. `css/common-green.css` の `rgba(3,232,34,,0)` 等（6箇所）と `css/style_add-green.css` の `rgba(148,248,161,, 0.3)` のカンマ二重を修正。青側 `common.css` にも同じパターンが無いか確認して同様に直す。
> 4. index.html 1001行目付近の `</dvi>` タイプミスを修正（前後のdiv構造の対応を確認してから直す）。
> 5. 全ページのフッター `&#995511;` を `&copy;` に修正。
> 6. beginner.php の Q5 のラベル `for="q-income"` が Q4 と重複している（selectのidは `q-job`）。`for="q-job"` に修正。
**確認**: 全ページが表示崩れなく開く。アンケートが送信まで通る。

## T1. viewportメタの統一（最優先・低リスク）
**目的**: 全ページで一貫したビューポート、かつユーザーのズームを許可（アクセシビリティ）。
▶ 指示（コピペ用）:
> `index.html`, `result.php`, `beginner.php`, `beginner_result.php`, `redirect.php`, `operationinfo.php` の `<meta name="viewport">` を
> `<meta name="viewport" content="width=device-width, initial-scale=1">` に統一して。`user-scalable=no` と `maximum-scale` は削除。他は変更しない。
> なお各ページに viewport メタが**3つ重複**している（24行目付近・32行目付近・33行目付近）。1つだけ残して残りは削除。
**確認**: 各ページでピンチズームができること。レイアウトが変わらないこと。

## T2. 極小端末(320px)での崩れ潰し
**目的**: iPhone SE等320px幅でも横スクロールを出さない。
▶ 指示:
> 320px幅で全4ページ（index/result/beginner/beginner_result）を表示し、`document.documentElement.scrollWidth > clientWidth` になる原因要素を特定。
> 固定 `width:○○px` が原因なら `max-width:100%` / `%` / `clamp()` に置き換えて横あふれを解消。**アンケートの構造とidは触らない**。差分は最小に。
**確認**: 320/375/390/414px で `scrollWidth == clientWidth`。

## T3. タップ領域の最適化
**目的**: 主要ボタン/リンクを押しやすく（モバイルUX）。
▶ 指示:
> モバイル(≤768px)で、CTAボタン（「この条件で探す」「詳細はこちら」）とQ1会社選択ボタン、アコーディオン行の**タップ可能領域が縦44px以上**になるよう padding/min-height を調整。
> 見た目の詰まりも改善（余白・行間）。青CSSを直したら緑CSSにも同じ調整を反映。
**確認**: 各ボタンの実測高さ≥44px。誤タップしにくいこと。

## T4. ファーストビューの情報設計
**目的**: スマホで「何のサイトか」「次に何をするか」が一目で伝わる／CTAを早く見せる。
▶ 指示:
> モバイルのファーストビュー（ヒーロー〜検索ボックス）を見直し。ヒーロー画像の高さ、キャッチと診断への導線、検索ボックスの見え方を調整して、
> **スクロールなしで「診断を始める」動線が伝わる**ようにする。既存の設問UI/idは維持。過度な作り替えはしない。
**確認**: 375px・414pxのファーストビュー内に診断開始要素が入る。

## T5. 比較表・ランキングのモバイル可読性
**目的**: 情報が多いセクションをスマホで読みやすく。
▶ 指示:
> 「おすすめカードローンを徹底比較」以下の比較表・各社カードを、モバイルで**横あふれ/文字潰れが無く、視線誘導が自然**になるよう整える
> （必要なら列の折り返し、フォントサイズ・余白の調整、重要数値の強調）。数値やPR表記の内容は変えない。青/緑両テーマで確認。
> 実装アイディア: 比較表は**1列目（項目名th）を `position: sticky; left: 0` で固定**し、表本体は横スクロール＋`scroll-snap`。右端に「→スクロールできます」のヒント（グラデーション or アイコン）を出すと3社比較が破綻しない。
**確認**: 比較表がモバイルで破綻なく読める。CTA「詳細はこちら」が目立つ。

## T6. 追従CTA（フォローバナー）の最適化
**目的**: スクロール中も申込導線を維持し、CVRを底上げ。
▶ 指示:
> 追従バナー（`btn_follow_experience.png` / `btn_follow_beginner.png`、`#follow-banner`）が、モバイルで本文を隠しすぎず、
> かつ常に押しやすい位置・サイズになるよう調整（`follow_banner.js` の挙動は壊さない）。
**確認**: スクロール時に追従CTAが適切に表示・タップできる。

## T7. タイポグラフィと余白のリズム
**目的**: 全体の読みやすさ・高級感を底上げ。
▶ 指示:
> 見出し/本文の font-size・line-height・段落間余白をモバイル基準で整え、セクション間の余白リズムを統一。
> フォントは既存（本文 M PLUS Rounded 1c / 見出し Zen Maru Gothic）を維持。青/緑両方に反映。
**確認**: 文字が詰まって見えない。セクション区切りが明快。

## T8. 画像パフォーマンス（表示速度）
**目的**: モバイル回線でも速く表示。
▶ 指示:
> ファーストビュー以外の `<img>` に `loading="lazy"` を付与し、主要画像に `width`/`height`（またはCSSでアスペクト比）を設定してレイアウトシフト(CLS)を防ぐ。
> 重い画像（例 `images/step.jpg` 等）はJPG化/圧縮で軽量化（画質は保つ）。**メタデータは除去**。
**確認**: 初回表示が体感で速い。スクロール時にガタつかない。

---

# アンケート改善（配置・表示内容）— 2026-07-07 追加

アンケート＝このLPの主CV導線。**id/name/liの順序/段階表示ロジックは変えず**、見せ方だけを改善する。
経験者フロー: エントリーモーダル（利用経験の分岐）→ Q1会社チェック → Q2〜Q8モーダル診断。
初心者フロー: モーダル分岐なし、Q1〜Q6のみ（`priority` あり）。

## T9. 診断モーダルに進捗表示を追加（回答完走率アップ）
**目的**: 「あと何問か」が見えないと途中離脱しやすい。進捗が見えると完走率が上がる。
▶ 指示（コピペ用）:
> `index.html` と `beginner.php` の診断モーダル（`#survey-modal`）の `.modal-header` に、現在の設問番号と総設問数（例「質問 3 / 8」＋細いプログレスバー）を表示して。
> 番号は `ul.survey-list li` の index から算出（indexは既にモーダルJS内で参照している）。**select の id/name、liの順序、既存の段階表示ロジックは変更しない**。表示の追加のみ。青/緑両方に反映。
**確認**: モーダルで設問を進める/戻ると進捗表示が追随する。送信まで通る。

## T10. 未回答表示の文言と回答済みの視覚化
**目的**: 一覧に「未回答」「なし」と並ぶのは消極的な印象で、どこを押せばいいかも伝わりにくい。
▶ 指示:
> `ul.survey-list` の各行について、(1) 初期表示の「未回答」「なし」を「タップして選択 ▼」系のアクション文言に変更（※ただしJSが `p` のテキストと `option:selected` のテキストを比較して開閉判定している箇所があるため、**比較ロジックに影響しないことを先に確認**し、必要ならJS側の比較も同時に整合させる）。(2) 回答済みの行には✓アイコン＋アクセント色の左ボーダーを付けて「答えた感」を出す。
> 青/緑両テーマに反映。差分は最小。
**確認**: 初期2問→回答→次の設問が開く挙動が壊れていない。回答済み行が一目で分かる。

- 注意: 現行仕様では 旧連番上の中間設問は欠番。既存JS参照のみを理由にプロダクトHTMLへ無理に復元しない。

## T11. エントリーモーダル（index.html）の体感改善
**目的**: 最初の分岐モーダルはLPの玄関。待ち時間と説得力を最適化する。
▶ 指示:
> `index.html` のエントリーモーダル（`.entry-modal`）について:
> 1. 分岐後の擬似ローディング `setTimeout(..., 3000)` を **1200ms** に短縮（2箇所）。ローディング文言はそのまま。
> 2. ボタンの視覚階層を調整: このページ（経験者向け）では「経験がある」を主ボタン（大きめ・強色）、「はじめて」を副ボタンに。クラス名（`.entry-first`/`.entry-experienced`）と遷移ロジックは変更しない。
> 3. 質問文の下に小さく「回答に合わせて最適な診断に切り替えます」等の一言を追加し、聞く理由を伝える。
**確認**: 両ボタンで正しく遷移（はじめて→beginner.php / 経験がある→クエリ付き再読込）。クエリパラメータが維持される。

## T12. 診断開始導線の強化（ファーストビューから1タップ）
**目的**: スマホのファーストビューから「診断を始める」までの距離を縮める（T4の具体策）。
▶ 指示:
> `index.html` と `beginner.php` のヒーロー直下（`#mainvisual` の直後）に「かんたん30秒診断をはじめる ▶」ボタンを追加。タップで `ul.survey-list` の最初の `li`（`.selected`）の click をトリガーし、**既存の設問モーダルをそのまま開く**（新しい診断UIは作らない）。
> ボタンは幅90%・高さ56px程度・テーマ色で目立たせる。青/緑両方。
**確認**: ボタンタップで Q2（経験者）/ Q1（初心者）のモーダルが開き、そのまま送信まで通る。

## T13. Q1「借入中の会社」チェックボックスのピルUI化（index.htmlのみ）
**目的**: 最初に目に入る設問の押しやすさ＝診断開始率に直結。
▶ 指示:
> `index.html` の `.loan-check` 内チェックボックス6個を、モバイルで**2列グリッドの大きなピル型ボタン**（高さ44px以上、角丸、選択時はテーマ色反転＋✓）に見せるCSSを追加。
> **input の name="current_loans[]" と value は変更しない**（labelで包む現構造を活かし、CSSのみで実現）。モーダル内に複製されたチェックUI（`.modal-check`）も同様に押しやすく。
**確認**: 複数選択が正しく動き、選択状態が送信クエリに反映される。

---

# モバイル微調整（追加）

## T14. iOS実機での細部最適化
**目的**: iPhone実機特有の不快挙動を潰す。
▶ 指示:
> 1. フォーム部品（select, input, button）のモバイル時 `font-size` を **16px以上**に統一（iOSのフォーカス時自動ズームを防ぐ。T1でズーム許可した後も入力時の勝手なズームは防ぎたい）。
> 2. 追従バナー `#follow-banner` と「PAGE TOP」に `padding-bottom: env(safe-area-inset-bottom)` を考慮（ホームバー端末で押せない/被る問題の予防）。
> 3. モーダル（`.entry-modal`, `#survey-modal`）の高さ指定に `100vh` があれば `100dvh` へ（URLバー分のズレ防止。フォールバックとして `100vh` は残す）。
> 4. タップ時のハイライトが不自然な要素には `-webkit-tap-highlight-color: transparent` を付与し、代わりに `:active` スタイルを用意。
> 青/緑両方に反映。
**確認**: 375px幅＋タッチエミュレーションで、select タップ時にズームしない・追従UIが下端で被らない。

---

# リデザイントラック（案A クリーン・フィンテック）— 2026-07-07 追加

**ゴール**: 白基調＋余白＋大きな文字の「今どきの金融サービス」の見た目に一新する。
**方式**: 既存CSSは触らず、後読みの `css/theme-v3.css`（青）/ `css/theme-v3-green.css`（緑）で上書き（詳細は `AGENTS.md` の特例ルール）。
**対象**: 全6ページ（index / result / beginner / beginner_result / operationinfo / redirect）。
**進め方**: R0→R11 の順。まず index.html（青）で作り込み → 確認 → 同じ変更を beginner.php（緑）へ → 結果ページ2枚 → 周辺ページ。

## R0. デザイントークンとベースの新設
▶ 指示（コピペ用）:
> `css/theme-v3.css` と `css/theme-v3-green.css` を新規作成し、全6ページの `</head>` 直前（既存CSSより後）で読み込んで。青ページには theme-v3.css、緑ページ（beginner系）には theme-v3-green.css。中身は共通構造で、CSS変数だけ差し替え:
> ```css
> :root {
>   --v3-accent: #185FA5;        /* 緑版: #0F6E56 */
>   --v3-accent-strong: #0C447C; /* 緑版: #085041 */
>   --v3-accent-weak: #E6F1FB;   /* 緑版: #E1F5EE */
>   --v3-amber: #EF9F27; --v3-amber-text: #633806;
>   --v3-text: #1B1B18; --v3-text-sub: #75736C;
>   --v3-bg: #FFFFFF; --v3-bg-alt: #F6F7F9; --v3-border: #E8EAED;
>   --v3-radius: 16px; --v3-radius-sm: 10px; --v3-radius-pill: 999px;
>   --v3-shadow: 0 1px 3px rgba(16,24,40,.07);
> }
> ```
> ベース上書き: body の背景を `--v3-bg`（旧CMS由来の壁紙画像は無効化）、フォントを Noto Sans JP（Google Fonts で 400/500/700 を追加読み込み。既存の M PLUS Rounded 1c / Zen Maru Gothic 指定より優先させる）、本文 16px / line-height 1.7、見出しは太字＋字間 -0.01em。リンク・ボタンの基本形（ピル型・高さ52px・アクセント塗り）もここで定義。
**確認**: 全ページで背景が白ベースになり、フォントが切り替わる。崩れた箇所はメモして以降のRタスクで対処。

## R1. ヘッダー刷新
▶ 指示:
> ヘッダーを「白背景・下ボーダー1px・ロゴ左・PR表記は右端に小さく」のスリムな固定ヘッダー（高さ56px程度、`position: sticky`）に。
> あわせて各ページの**スクロールでヘッダー背景をオレンジにするJS（`rgba(252,128,3,...)` を設定している `$(window).scroll` ブロック）を削除**して（新デザインと競合するインラインstyleを吐くため。T0で修正済みでも削除してよい）。ロゴ画像はそのまま。
**確認**: スクロールしてもヘッダーが白のまま追従。全6ページ。

## R2. ヒーローのHTML化（最重要・印象の8割）
▶ 指示:
> `index.html` の `#mainvisual`（`headertitle.png`）と `beginner.php` の `#mainvisual`（`header_beginner.jpg`）を、画像をやめてHTML構成に置き換え:
> 1. 上部にピルバッジ「最短20分で借入まで」（`--v3-accent-weak` 地に `--v3-accent-strong` 文字）
> 2. H1: 経験者版「あなたに合うカードローンが3問でわかる」/ 初心者版「はじめてでも安心。あなたに合うカードローンが3問でわかる」（コピーは調整可。誇大にしない）
> 3. サブ: 「大手3社を条件で比較・診断は無料」
> 4. 主CTA: 「かんたん30秒診断をはじめる」ピルボタン（幅90%上限420px・高さ56px・`--v3-accent` 塗り白文字）。タップで `ul.survey-list` の最初の li の click をトリガーして既存の設問モーダルを開く（=T12と同一実装。T12実施済みならそれを流用）
> 5. CTA下に信頼チップ3つ: 「来店不要」「スマホ完結」「診断無料」（小さなアウトラインピル）
> 背景は白、下に `--v3-bg-alt` のセクション区切り。`#mainvisual` の id は維持。旧画像ファイルは削除しない。
**確認**: 375px でファーストビュー内にH1とCTAが収まる。CTAからアンケート→送信まで通る。

## R3. 診断ボックスとモーダルのスキン刷新
▶ 指示:
> `#search_box` を白カード（radius 16px・border 1px `--v3-border`・shadow）に載せ替え、見出し「あなたにぴったりのカードローンを検索してみる」→「かんたん診断で絞り込む」に短縮。
> `ul.survey-list` の各行: 高さ56px以上・行間に8px・回答済み行は左に4pxのアクセントボーダー＋✓。「この条件で探す」ボタンはピル型・全幅。
> `#survey-modal` と `.entry-modal`: 白背景・radius 20px・選択肢を高さ52pxのピルボタン縦積みに。選択中は `--v3-accent-weak` 地＋`--v3-accent` ボーダー。プログレスバー（T9）もこのスキンに合わせる。
> タイマー（`.deadline-box`）は「⏰ 今日中に借りるなら ○○:○○ までに申込」の1行スリム通知バー（`--v3-amber` 系配色）に。
> **CSSのみで実現し、フォームの id/name/構造・JSロジックは変更しない。**
**確認**: 診断を最初から送信まで通し、両テーマで見た目が統一されている。
【関連】T3・T9・T10・T13はこのタスクに統合してよい。

## R4. 比較表の刷新
▶ 指示:
> `.c-compareTable` を: 外枠なし・行間ヘアライン（1px `--v3-border`）のみ・1列目(th)は `position: sticky; left: 0; background: #fff`・横スクロール＋`scroll-snap`・右端にスクロールヒント。
> ◎○表示（`isDoubleCircle`/`isCircle`）は「◎最短20分」のようにアクセント色の小バッジ＋太字数値に置き換え（CSSの `::before` 装飾を変更。td内テキストは変えない）。
> 「詳細」ボタンはピル型 `--v3-accent` 塗り。注釈（`.caution`）は 12px グレーのまま。
**確認**: 320px で表が破綻せず、1列目固定で横スクロールできる。数値・注釈の文言は不変。
【関連】T5はこのタスクに統合。

## R5. ランキングカードの刷新
▶ 指示:
> `.lp-box.case` のランキングカードを白カード化（radius 16px・border・shadow・内側padding 20px）し、画像装飾をHTMLに置換:
> 1. `rank1_catch.png` → カード左上の「No.1」アンバーバッジ＋「一番選ばれています」テキスト
> 2. `hitokoto_point__*.png`（ひとことポイント画像）→ アクセント色の吹き出し風テキストブロック（文言は画像内と同等の内容で簡潔に）
> 3. スペック表（`.reco_table`）→ 3つのメトリクスタイル（融資時間/実質年率/限度額。`--v3-bg-alt` 地・ラベル12px・数値18px太字）
> 4. `.features` のリスト → チップ（来店不要/Web申込/土日祝可 等。`li.off` はグレーアウトのまま）
> 5. 「ここがオススメ」「リアルな体験談」の details → フラットなアコーディオン（chevron付き）。口コミ画像はカード内にそのまま
> 6. 「詳細はこちら」→ 全幅ピルCTA＋上に小さくマイクロコピー（既存spanの文言を使用）
> アフィバナー画像・PR表記・注釈文言は不変。redirect.php へのリンクも不変。index → beginner → 結果ページ2枚の順で全カードに適用。
**確認**: 3社カードが両テーマ・全ページで統一。リンクとPR表記が全て残っている。

## R6. 結果ページの情報設計刷新（result.php / beginner_result.php）
▶ 指示:
> 1. 結果ヘッダー（`.result-summary`）: 「あなたの条件に合うカードローン **3件**」を大きく、その下に選択条件（`#selected-amount` 等）を**削除可能風の条件チップ**の見た目で横並び（機能は現状のテキスト表示のまま）。「条件を変更する」はアウトラインピルボタンで隣接配置。
> 2. `result_after.png` の画像バナーをHTMLの案内ブロック（「診断結果にもとづくおすすめ順です」等の1〜2行）に置き換え。
> 3. **並び替えプルダウンは現在未配線**（selectに`name`が無くJSは `name='sort'` を探している）。機能させないなら非表示に、残すなら実装を配線（`?order_by=` 等は無いので、DOM上でカードを並び替えるJSを追加）。どちらにするかはコミットメッセージに明記。
> 4. ページ下部の再診断フォーム（`#search_box`）の上に「条件を変えてもう一度診断」の見出しを付け、R3のスキンを適用。
**確認**: LP→診断→結果ページの流れで選択条件が正しくチップ表示される。「条件を変更する」で下部フォームへスクロールする。

## R7. redirect.php（中継ページ）の刷新
▶ 指示:
> 中継ページを「ブランドロゴ → スピナー → 『アコムのサイトへ移動中です』 → 移動先バナー → 『移動しない場合はこちら』」の縦1カラム・中央寄せのクリーンな画面に。theme-v3 を読み込みトークンを使用。
> `.deadline-box`（カウントダウン）は1秒で遷移するページには不要なので削除。**転送ロジック（linkMap / getStoredQueryParams / setTimeout 1000ms / fallback-link）は一切変更しない。**
**確認**: `?item=acom` 等でバナーが出て1秒後に遷移（プレースホルダーURLのためエラー遷移で可）。fallbackリンクが表示される。

## R8. operationinfo.php（運営者情報）の刷新
▶ 指示:
> コンテンツを白カード内の定義リスト型レイアウトに: ページタイトル「運営会社情報」、項目（運営会社/住所/事業内容/設立/お問い合わせ）を `dl` 風の2カラム（モバイルは縦積み）で整列。ページ上部に「← トップに戻る」リンク。
> プレースホルダー（`__COMPANY_NAME__` 等）はそのまま残す。theme-v3 適用でヘッダー/フッターは他ページと統一。
**確認**: モバイルで読みやすく、他ページとトーンが揃っている。

## R9. フッターの刷新
▶ 指示:
> 全ページ共通フッターを: 上段=ロゴ＋一言、中段=「本サイトの目的」「各ローン会社の概要」「注意事項」を**モバイルではアコーディオン**（PCでは3カラム展開）に、下段=運営社情報リンク＋コピーライトバー。
> **文言は一切削らない**（コンプライアンス表記のため）。開閉UIで畳むだけ。details/summary で実装しJS追加は不要。
**確認**: 全文言が残っており、モバイルでフッターが画面3枚分→1枚分程度に収まる。

## R10. 残りの画像見出しのHTML化と3ステップ図の再構築
▶ 指示:
> 1. `title_osusume.png`（当サイトオススメ）、`beginner_title_osusume_ranking.png`、`beginner_title_flow.png`、`title_sokujitu.png`、`title_kuchikomi.png`、`recommend_catch.png`、`mordalhead.png` 等の見出し画像を、同等文言のHTML見出し（h2＋装飾）に置き換え。
> 2. `step.jpg`（358KB）の申込3ステップ図を、番号付きHTMLステップコンポーネント（①申込→②審査→③借入、各にアイコンと1行説明）に再構築して画像を撤去。
> 3. 残った `<img>` に `loading="lazy"` と `width`/`height` を付与（T8の内容をここで実施）。
**確認**: 画像見出しが消えテキスト化。Lighthouse等でCLS/転送量が改善。
【関連】T8はこのタスクに統合。

## R11. 総合回帰チェックとロールバック確認
▶ 指示:
> 1. 全6ページ×両テーマを 320/375/390/414px＋PC幅で表示し、横スクロール・文字潰れ・タップ領域44px未満が無いことを確認。
> 2. アンケートを「エントリーモーダル→Q1→…→送信→結果ページ→詳細ボタン→redirect」までフルで2フロー（経験者/初心者）通す。
> 3. theme-v3 の link タグを一時的に外し、旧デザインに戻ることを確認（ロールバック検証）→ 戻す。
> 4. 未使用になった画像の一覧をコミットメッセージに記録（削除はしない）。
**確認**: 上記すべてパス。

---

## 進め方のルール
- 1タスク完了ごとに、AGENTS.mdの「確認方法」を実施し、**アンケートが送信まで通ること**を回帰確認。
- 迷ったら「壊してはいけないもの」を優先し、差分は最小に。
- 実データ（社名/GTM/アフィリンク）が来たら別途プレースホルダーを流し込む（このbacklogとは分離）。
